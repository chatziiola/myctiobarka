/ Comments on ncurses:
// -- Screen/window/pad(!) manipulation (NEED IT); 
// output to windows and pads; 
// -- reading temrinal input (NEED IT) ; 
// control over terminal and curses input and output options;
// environment query routines(!); 
// -- color manipulation (NEED IT);
// use of soft label keys(!)
// terminfo capabilities
// low level terminal-manipulation routines
#include <curses.h>
#include <stdio.h>

int main()
{
	printf("%s\n","Hello world!");
	// There is a default windows called stdscr (the size of term screen)j
	// Window routines: move, addch
	j
}
